[pytest]
# Ubuntu 22.04 provides pytest 6.2.5, while user-local Python packages may
# register plugins requiring a newer pytest.  Block the incompatible AnyIO
# entry point before pytest performs third-party plugin auto-discovery.
addopts = -p no:anyio
